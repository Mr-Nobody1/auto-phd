# PhDApply AutoGen Service

Python FastAPI + Microsoft AutoGen backend used by the main PhDApply app.

Run directly:

```bash
python -m uvicorn app.main:app --app-dir autogen_service --reload --port 8001
```
